  query ($id: Long!, $from: String!, $to: String!) {
	restaurant: getRestaurant(id: $id) {
        id
        name
		weekdayMenus(startDate: $from, endDate: $to) {
			id
			date
			menuItems {
				id
				title
				content
				price
				currency
			}
		}
	}
  }