{
  "name": "return-of-the-menu",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "start": "npx @11ty/eleventy --serve --incremental",
    "build": "npx @11ty/eleventy"
  },
  "engines": {
    "node": ">=18.14.1"
  }
}
