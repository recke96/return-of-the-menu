{
  "name": "return-of-the-menu",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "start": "npx @11ty/eleventy --serve --incremental",
    "build": "npx @11ty/eleventy"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-fetch": "^4.0.0",
    "chota": "^0.9.2",
    "prettier": "^3.0.3",
    "sanitize-html": "^2.11.0"
  }
}
